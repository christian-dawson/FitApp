import { TraineePipePipe } from './trainee-pipe.pipe';

describe('TraineePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TraineePipePipe();
    expect(pipe).toBeTruthy();
  });
});
